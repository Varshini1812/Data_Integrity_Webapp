[build]
  command = "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
  publish = "staticfiles"

[build.environment]
  DJANGO_SETTINGS_MODULE = "cloudsafe.settings"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/django_handler"
  status = 200